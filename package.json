{
  "name": "m3u8-fetcher",
  "version": "1.0.0",
  "description": "A Dockerized service to fetch M3U8 URLs.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  }
}
